# Labour Dorm 優化計劃（2026-07-30）

## 1. 目標

把 `labour-dorm.com` 改成可信、容易查詢、真正支援中英文的香港勞工宿舍網站，並提供一個簡單但安全的 `/admin` 後台，讓管理員自行新增、編輯、下架及刪除房源和圖片。

本計劃涵蓋：

- UI/UX 與手機體驗
- 中英文內容
- 房源查詢及 WhatsApp 轉換
- SEO、結構化資料與索引管理
- 圖片效能
- `/admin` 身分驗證及房源管理

## 2. 開發及資料安全原則

- 所有開發在獨立 branch／worktree 進行，不直接改 production。
- 開發前先完成測試計劃、測試準則及必要的自動測試。
- 開發階段只使用 Firebase Emulator、獨立測試 Firebase project 或 mock data。
- 不在開發階段新增、修改或刪除正式 Firestore／Storage 資料。
- 最後接駁正式 Firebase 前，先備份 Firestore 和 Storage 清單，再做小範圍驗收。
- 不把密碼、Firebase Admin SDK key、service account 或其他密鑰寫入 Git。

## 3. 現況診斷摘要

### 3.1 信任及轉換

- 英文頁只有 metadata 是英文，大部分可見內容仍是中文。
- 公開導覽列顯示「管理」和「登入」，不適合一般租客介面。
- 房源頁的「立即預訂」沒有完成提交流程，卻顯示服務費和清潔費。
- About 頁包含「政府認可」「10,000+ 滿意住客」「500+ 合作房源」等未驗證聲明。
- 評分、評論數及「超讚房東」目前是程式內固定資料，並輸出成搜尋引擎的 AggregateRating schema。

處理原則：未有可核對來源的聲明、評分或評論先移除，不以示範資料冒充正式資料。

### 3.2 UI/UX

- 首頁 Hero 佔滿一個螢幕，房源列表在手機首屏以下。
- 語言切換元件已存在，但沒有放進 Header。
- 房源卡包含巢狀按鈕和連結，鍵盤操作及無障礙語意不完整。
- 圖片切換依賴 hover，手機難以使用；部分按鈕小於 44px。
- 現有「客人／睡房／浴室」模型不符合勞工床位租賃。
- 手機相簿固定四欄，容易擁擠。

### 3.3 SEO 及技術

- Sitemap 漏收首頁可見的 property 13、14。
- Build 產生的公開房源頁、首頁房源和 sitemap 使用不同篩選邏輯。
- 英文頁與中文頁內容高度重複，hreflang 不能彌補未完成的翻譯。
- 房源描述模板化，缺乏交通、床位、入住條件及實際設施資料。
- 圖片總量約 36MB，個別原圖超過 3MB；靜態輸出沒有 Next Image 最佳化。
- `www.labour-dorm.com` 尚未正確提供 SSL／轉址。
- 根目錄目前以 302 跳到 `/zh-hk/`。
- TypeScript 檢查有多項錯誤，現有 build 設定會跳過 type-check 和 lint。

## 4. `/admin` 後台需求

### 4.1 入口和登入

- 統一入口：`https://labour-dorm.com/admin`
- 未登入時自動進入 `/admin/login`。
- 使用 Firebase Authentication 的 Email/Password 登入。
- 指定管理員帳號：`arrivals@hkflal.com`
- 初始密碼使用產品負責人已提供的值，在 Firebase Console 或受控的初始化流程中建立；明文密碼不得寫入本文件、程式碼、Git commit 或前端環境變數。
- 首次登入後應更換初始密碼；之後提供「忘記密碼」電郵重設。
- 公開網站 Header 不顯示管理入口。管理員直接輸入 `/admin` 進入。

### 4.2 權限

- 不能只靠前端隱藏按鈕保護資料。
- Firebase Auth 登入後，仍需由 Firestore Rules 和 Storage Rules 驗證權限。
- 建議使用 Firebase custom claim：`admin: true`。
- 初期可同時核對指定 email，custom claim 完成後以 claim 為主要權限來源。
- 未登入或非管理員：
  - 可讀取已發布房源。
  - 不可新增、修改、刪除房源。
  - 不可上載或刪除圖片。
  - 不可讀取草稿、內部備註或審計記錄。

由於網站使用靜態輸出，真正的安全邊界必須放在 Firebase Security Rules，不能依賴 Next.js 頁面路由。

### 4.3 房源列表

管理員登入後先看到單頁 Dashboard：

- 搜尋：房源名稱、地址、property ID
- 篩選：已發布、草稿、下架、地區、性別、可入住日期
- 排序：最近更新、月租、可入住日期
- 每列顯示封面、名稱、地區、月租、床位、性別、狀態及最後更新時間
- 快速操作：編輯、複製、預覽、下架
- 新增房源按鈕保持明顯，但刪除不放在主要操作位置

### 4.4 新增及編輯房源

表單至少包括：

- Property ID
- 中英文房源名稱
- 中英文簡介及完整描述
- 地址、地區、附近港鐵站
- 月租、按金、其他費用
- 總床位、可用床位
- 指定性別
- 最早入住日期
- 最短租期
- 房間／床型
- 設施
- WhatsApp 查詢資料
- 發布狀態：草稿、已發布、下架
- SEO title、meta description、圖片 alt text

表單要求：

- 必填欄位有清晰標示。
- 錯誤顯示在相關欄位旁。
- 離開未儲存表單前要提示。
- 儲存期間鎖定重複提交並顯示進度。
- 成功或失敗後提供明確結果及恢復方法。

### 4.5 圖片管理

- 支援拖放及檔案選擇。
- 支援 JPG、PNG、WebP；上載前檢查格式及大小。
- 自動產生 WebP／AVIF 或經壓縮的顯示版本。
- 可設定封面、重新排序、編輯中英文 alt text。
- 顯示上載進度、失敗重試和圖片預覽。
- 圖片路徑使用 `properties/{propertyId}/{generatedFileName}`。
- 移除圖片時先更新房源記錄，再清理 Storage 檔案，避免房源引用不存在的圖片。
- 刪除房源時要處理孤兒圖片，並留下可核對的結果。

### 4.6 刪除策略

- 一般操作先使用「下架／封存」，房源不再公開但保留資料。
- 永久刪除放在獨立危險區。
- 永久刪除前顯示房源名稱、Property ID、圖片數量及影響。
- 管理員需要再次輸入房源名稱或 Property ID 確認。
- 刪除後寫入 audit log；如果技術上可行，先保留可恢復快照。

## 5. 公開網站優化

### 第一批：信任和轉換

- 移除未驗證評分、評論及 About 聲明。
- 公開 Header 移除管理和登入。
- 把不能完成的預訂元件改成 WhatsApp 查詢。
- WhatsApp 訊息自動帶入房源、入住日期、人數及目前頁面 URL。
- Hero 縮短，讓手機首屏看到篩選入口或第一個房源。

### 第二批：房源體驗

- 首頁提供地區、性別、入住日期及床位類型篩選。
- 房源卡優先顯示月租、可入住日期、性別、可用床位及港鐵位置。
- 房源詳情改用勞工住宿用語，不再顯示不適合的 Airbnb 式欄位。
- 手機相簿改成單張主圖加滑動瀏覽。
- 所有按鈕至少 44×44px，補上 aria-label、focus state 和鍵盤操作。

### 第三批：中英文

- Header、Hero、Footer、About、房源詳情及表單全部接入翻譯。
- 每個房源保存中英文名稱、地址說明及描述。
- `/en/` 頁面除了不可翻譯的正式名稱外，不應混入中文介面文字。

## 6. SEO 優化

- 建立單一 `isIndexableProperty()` 規則，首頁、靜態頁及 sitemap 共用。
- 草稿、下架和資料不足的房源不輸出公開頁，或明確使用 noindex。
- Sitemap 包含所有有效公開頁，並使用真實 `updatedAt`。
- 根目錄如固定以中文為預設，改用 301 前往 `/zh-hk/`。
- 完成 `www` SSL，並將 www 301 到非 www。
- 首頁加入 Organization 和 WebSite schema。
- 只有存在真實評論來源時才輸出 AggregateRating。
- 加入 Open Graph 圖片及完整的分享預覽。
- 建立地區落地頁，例如旺角、銅鑼灣；內容必須有實際房源和獨有資料。
- 圖片使用描述性檔名、穩定尺寸和中英文 alt text。
- 修復 TypeScript／lint，取消 production build 的錯誤忽略設定。

## 7. 開發前測試計劃

### Auth

- 未登入直接開 `/admin` 會進入登入頁。
- 錯誤密碼不會透露帳號是否存在。
- 非管理員即使手動呼叫 Firestore／Storage API 也會被 Rules 拒絕。
- 管理員登入、登出、重設密碼及 session 恢復正常。

### 房源 CRUD

- 在測試 Firebase 新增房源後可預覽，但不影響 production。
- 編輯文字、價格、狀態及床位資料後正確保存。
- 下架後公開列表和 sitemap 不再出現。
- 永久刪除需要二次確認，並正確處理圖片。
- 重複提交、網絡中斷及部分圖片上載失敗都有可恢復結果。

### 圖片

- 檔案格式、大小及數量限制有效。
- 封面和排序可保存。
- 失敗上載不會留下錯誤的房源引用。
- 已發布頁的圖片不變形、沒有明顯 layout shift。

### UI/UX

- 測試 375px、768px、1024px、1440px。
- 沒有水平溢出或被固定導覽遮擋的內容。
- 所有主要操作可用鍵盤完成。
- Modal 開啟後正確管理焦點，Escape 可關閉。
- 英文頁不出現未預期的中文 UI。

### SEO

- 每個可索引頁只有一個 H1。
- Canonical、hreflang、title 和 description 正確。
- Sitemap 內所有 URL 回傳 200，沒有草稿或下架房源。
- 結構化資料通過驗證，沒有虛假 AggregateRating。
- Production 以外環境使用 noindex。

## 8. 完成準則

- TypeScript、lint、測試和 production build 全部通過。
- 管理員可從 `/admin` 完成房源及圖片 CRUD。
- Firebase Rules 測試證明非管理員不能寫入。
- 開發和測試期間沒有寫入正式 Firebase。
- 手機沒有水平溢出，所有主要點擊區域至少 44px。
- `/en/` 完成真正英文內容。
- Sitemap、首頁與公開房源使用同一組索引規則。
- Lighthouse 目標：Accessibility ≥95、SEO ≥95、Performance ≥85。
- 最後接駁 production 前完成 Firestore／Storage 備份及人工驗收。

## 9. 本機實作交接及上線前步驟

本機 branch 已完成的內容包括公開網站的查詢介面、中英文可見內容、WhatsApp 查詢流程、靜態 SEO 頁、地區落地頁、Firebase Auth／Firestore／Storage 的管理端程式、圖片壓縮上載、Security Rules 及自動靜態網站驗證。

部署前仍必須由有 Firebase 專案權限的人員完成以下受控操作：

1. 複製 `.env.example` 為本機 `.env.local`，以 Emulator 或獨立測試 Firebase project 驗收；不要使用 production 資料做開發測試。
2. 在 Firebase Authentication 以受控方式建立指定管理員帳戶，強制首次登入後變更密碼，並設定密碼重設電郵流程。程式和 Git 不保存密碼。
3. 先將 `firestore.rules`、`storage.rules` 和 `firestore.indexes.json` 部署至測試專案，驗證非管理員無法寫入、公開查詢只能讀取 `active` 房源。
4. 備份正式 Firestore 和 Storage 清單後，才部署 Rules、Hosting 及建立／授權正式管理員。
5. 管理端發布後，公開首頁會從 Firestore 讀取已發布房源；但新增房源的獨立 SEO 詳情頁、地區頁和 sitemap 仍由靜態 build 產生，所以每次發布後需要重新 build／deploy 才會納入搜尋索引。
6. 在 Firebase Hosting 或 DNS 完成 `www.labour-dorm.com` 的 SSL 和 301 至 `labour-dorm.com`。這需要網域／Firebase Console 權限，不能由本機程式碼代替。
